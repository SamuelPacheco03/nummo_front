// Los hooks de reportes viven en features/reports/hooks (los usan el Panel e Informes).
// Se re-exportan aquí para no romper los imports relativos existentes del dashboard.
export * from '@/features/reports/hooks'
