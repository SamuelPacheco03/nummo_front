# Cargado SOLO en desarrollo (`pnpm dev`). `vite build` (producción) NO lo lee,
# así que estas variables nunca llegan al bundle de producción.

# Muestra el botón de "credenciales demo" en el login (comodidad de dev).
# En producción no se define → el botón queda oculto.
VITE_DEMO_LOGIN=true
